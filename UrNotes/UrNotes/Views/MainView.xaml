<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="UrNotes.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UrNotes.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:UrNotes.Views.UserControls.General"
    mc:Ignorable="d">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="8*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <uc:LeftPanelMenu x:Name="LeftPanelMenuComponent" DataContext="{Binding}" />
        <Grid Grid.Column="1" Margin="0">
            <TabView x:Name="NotesTabView"
                Margin="0 -8 0 0"
                AddTabButtonClick="NotesTabView_AddTabButtonClick"
                TabCloseRequested="NotesTabView_TabCloseRequested"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                >
            </TabView>
        </Grid>
    </Grid>
</UserControl>
