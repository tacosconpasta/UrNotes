<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="UrNotes.Views.UserControls.Buttons.SaveStatusBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UrNotes.Views.UserControls.Buttons"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Manual save button on the left, then the spinner and the save state text -->
    <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="10 2 10 4">
        <Button x:Name="SaveButton"
                Padding="0"
                Margin="0"
                Background="Transparent"
                BorderThickness="0"
                AutomationProperties.Name="Save this note now"
                Click="SaveButton_Click">
            <ToolTipService.ToolTip>
                <ToolTip Content="Save now" />
            </ToolTipService.ToolTip>
            <!-- Explicit color so the glyph looks the same on every save state (enabled/disabled) -->
            <FontIcon Glyph="&#xE74E;"
                      FontFamily="Segoe MDL2 Assets"
                      FontSize="12"
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Button>
        <ProgressRing x:Name="SavingSpinner" Width="12" Height="12" IsActive="False" Visibility="Collapsed" />
        <TextBlock x:Name="StatusText"
                   FontSize="12"
                   VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</UserControl>
